using UnityEngine;
using System.Collections;

//TODO  determine if GUI health managemtn and plain game logic health management
//		should be 2 classes? if not move this from GUI??

/**
 * All entities that needs to display health bars or scrolling
 * text should inherit from this class
 */ 
public class HealthBar : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
